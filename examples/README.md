# Examples

Each script runs standalone with `python examples/<name>.py` after `pip install -e .`.

- `campaign_summary.py` — a fake model names the wrong campaign as the winner; `superlative` raises `UnsupportedClaim`, then the corrected output passes.
